# POCKET first-class product bar

**Version:** 2.1.0  
**Lab:** ItsNotAI Labs / Medina Tech Labs

## What “first-class” means here

Not a demo. A **sovereign host co-pilot** that:

1. Ships real code and products on **your** machine  
2. Isolates **market seats** from founder disk  
3. Runs **multi-agent ship loops** that finish (plan→code→test→ship)  
4. Scales code context with **Infinite Wiki** (profile → slice)  
5. Runs **Cortex/Subcortex** dual-loop (talk while world updates)  
6. Keeps an **always-on swarm** pulsing work  
7. Exposes a **sellable API** with the same wiki inject  
8. Covers **phone**, **WSL**, **Work Studio**, **downloads** (researcher license)

## Live score

```http
GET /v1/class
GET /v1/ready
GET /health   → first_class, class grade
```

## Surfaces

| Surface | Path |
|---------|------|
| Desk | `/desk` |
| Phone | `/phone` |
| Work Studio | `/work` |
| Infinite Wiki (agent) | desk mode `wiki` |
| API | `/developers` · `POST /v1/ai/chat` |
| Downloads | `/download` |

## Doctrine

- Sovereign host — not cloud lock-in  
- Founder machine ≠ market seat  
- Cortex talks; Subcortex works silently  
- Never dump 10k-line files into models  
- Swarm keeps the lab shipping  

Grade **A/S** = first-class bar met.
