# Assets

Publication-style figures generated by the same **construct** engine the app uses (`science_construct.py`).

Regenerate:

```powershell
$env:PYTHONPATH = "$PWD\src"
python -c "from pocket.science_construct import chart_titration; ..."
```

Or via API: `POST /v1/researchers/construct`
