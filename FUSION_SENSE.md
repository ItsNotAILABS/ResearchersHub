# Fusion-Sense Doctrine (product baseline)

**wf1 is the gold standard.** All product paths go through Fusion-Sense:

1. `perception.sense` / `GET /v1/vision/page`
2. `POST /v1/rfe/synthesize` (or fusion remake)
3. Act via vcomp / orchestrator with re-sense
4. Product demos via `device_remake` (lifelike phone/web), not desktop-in-bezel

Research: Documents/POCKET_Research/Fusion_Sense_Interface_Paradigm/
Tour: GET /tour
