# Legacy host scripts

These files were inherited from the older multi-agent host (POCKET names).

They are **not** the ResearchersHub public entrypoints.

Use instead:

- `../Start-ResearchersHub.ps1`
- `../Open-ResearchersHub-Edge.cmd`
- `../export_skills_catalog.py`

Kept only for operators who still co-host legacy tooling.
